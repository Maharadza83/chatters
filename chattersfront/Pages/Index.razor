@page "/"
@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        // Natychmiast przekieruj użytkownika do strony czatu.
        // Router w Main.razor sprawdzi, czy użytkownik jest zalogowany.
        // Jeśli nie jest, komponent RedirectToLogin przekieruje go dalej na stronę /login.
        Navigation.NavigateTo("/chat");
    }
}